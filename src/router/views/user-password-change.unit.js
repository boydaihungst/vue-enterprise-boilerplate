import UserPasswordChange from './user-password-change';

describe('@views/user-password-change', () => {
  it('is a valid view', () => {
    expect(UserPasswordChange).toBeAViewComponent();
  });
});
