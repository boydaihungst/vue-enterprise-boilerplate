version: '3.4'
services:
  archivefile-com-frontend:
    build:
      context: ../
      target: dev-stage
    volumes:
      - ../:/home/app
      - $HOME/.ssh:/home/node/.ssh
      - vscode-extension-volume:/home/node/.vscode-server/extensions
      - vscode-extension-volume-insiders:/home/node/.vscode-server-insiders/extensions
    working_dir: /home/app/
    restart: on-failure
    command: tail -f /dev/null
    user: node
    ports:
      - 3000:3000
      - 5858:5858
      - 80:80
      - 443:443
      - 8080:8080
    environment:
      - NODE_ENV=development
      - PORT=3000
    networks:
      default:
volumes:
  vscode-extension-volume:
  vscode-extension-volume-insiders:
